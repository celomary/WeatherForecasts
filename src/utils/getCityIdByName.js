//import cities from '../assets/cities_all.json';
/*
const getCityId = (cityName)=>{
    for (const city of cities.results)
    {
        if (city.city_name.toLowerCase() === cityName)
            return (city.city_id);
    }
    return (null);
}


export default getCityId;
*/